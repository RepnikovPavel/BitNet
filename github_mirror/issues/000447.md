# issue #447: sys.exit(1) runs unconditionally due to indentation bug in run_command()

- state: open
- author: @mango766
- created_at: 2026-03-13T10:30:25Z
- updated_at: 2026-03-21T15:38:05Z
- url: https://github.com/microsoft/BitNet/issues/447

---

Hi!

I think there's an indentation issue in `setup_env.py` (line 107) inside the `run_command` function:

```python
    else:
        try:
            subprocess.run(command, shell=shell, check=True)
        except subprocess.CalledProcessError as e:
            logging.error(f"Error occurred while running command: {e}")
        sys.exit(1)  # this is outside the except block
```

The `sys.exit(1)` is at the same indentation as the `try`, so it runs unconditionally after the subprocess finishes — even when the command succeeds. I believe it was meant to be inside the `except` block.

The same pattern is in `utils/e2e_benchmark.py` line 23.

Happy to send a PR to fix the indentation.


## Comments (3)

### Comment by @Jah-yee at 2026-03-13T19:50:06Z

I created a PR #456 that fixes this indentation issue in both setup_env.py and utils/e2e_benchmark.py. The sys.exit(1) has been moved inside the except block so it only runs on subprocess failure.

### Comment by @sudo-init at 2026-03-15T23:39:11Z

I independently found the same issue and opened #472 before noticing this one. Closing mine as duplicate.

While reviewing the same area, I found two more related issues that might be worth fixing together:

**`setup_env.py:212` — `exit(0)` on error condition:**
```python
if arch not in COMPILER_EXTRA_ARGS.keys():
    logging.error(f"Arch {arch} is not supported yet")
    exit(0)  # should be sys.exit(1)
```

**`setup_env.py:85` — unhandled KeyError:**
```python
def system_info():
    return platform.system(), ARCH_ALIAS[platform.machine()]
```
On unrecognized platforms (e.g. `i686`, `riscv64`), this raises `KeyError` before the architecture check at line 210 can provide a proper error message.

### Comment by @mvanhorn at 2026-03-21T15:38:05Z

Submitted a comprehensive fix in #504 that addresses all 5 error-handling bugs identified in this issue and the follow-up comments:

1. `sys.exit(1)` indentation in `setup_env.py:107` (original report)
2. Same indentation bug in `e2e_benchmark.py:23`
3. `exit(0)` on unsupported arch in `setup_env.py:212` (per @sudo-init's comment)
4. `ARCH_ALIAS` KeyError on unrecognized architectures at `setup_env.py:85`
5. `SUPPORTED_QUANT_TYPES` KeyError in `parse_args()` at `setup_env.py:225`

Total diff is 7 lines added, 4 removed across 2 files.
