# issue #420: suggestion concerning llama.cpp /GGUF Specs, replacing weight look-up

- state: open
- author: @jaynu2
- created_at: 2026-03-09T02:40:08Z
- updated_at: 2026-03-12T16:38:24Z
- url: https://github.com/microsoft/BitNet/issues/420

---

I have this idea to replace the current unpacking method.  I ran it past gemini pro, and it was pretty enthusiastic.  Basically, it is just using the loading the simd masks from the weights directly and using them to transfer the weights in two operations, rather than all the look-ups.  I'll attach the full chat.  This was the summary:
Conclusion
Your idea is valid and optimal for a specialized AVX-512 runtime. If you were writing a custom kernel solely for Xeon/Epyc servers, using Interleaved Bit-Sliced Storage + vpmovm2b expansion is likely the theoretical speed limit for weight decompression.
AI responses may include mistakes.Learn more
The weights could be interleaved ... first 64 bit value for -1, second 64 bit value for 1. The 0 values are cleared by the first transfer operation, which is an option for these type of transfers.
Yes, the technique you are describing is a known high-performance optimization called
Bit-Sliced (or Planar) Weight Expansion, and it is extremely effective on AVX-512 hardware.
Your logic is sound: by storing weights as two separate Bit Planes (one mask for -1, one for +1) and using Masked Move (vmov) instructions, you can bypass the shuffle bottleneck entirely and saturate the memory bandwidth. 
The Planar Optimization Strategy
The Mechanism: Masked Expansion
Your proposal replaces the complex "Shuffle & Unpack" logic with raw memory throughput. In AVX-512, the specific instruction that validates your "clearing 0s" logic is vpmovm2b (Move Mask to Byte). 
Step
	
Operation (AVX-512)
1. Load & Expand (-1)
	
vpmovm2b zmm0, k1

    Explanation: Expands the 64-bit mask k1 into bytes.
    Implicit Zeroing: If a bit is 1, it writes 0xFF (-1). If 0, it writes 0x00. This handles your "clear to 0" requirement in a single cycle.

2. Merge (+1)
	
vmovdqu8 zmm0 {k2}, [const_1]

    Explanation: Uses the second mask k2 as a Write Mask.
    Logic: It writes the value 1 only where k2 is set, leaving the existing -1 and 0 values untouched.

Performance Analysis
Why This Beats Lookups (The Port War)
The standard bitnet.cpp implementation relies on vpshufb (Shuffle) to unpack weights. On Intel CPUs (Skylake, Ice Lake, Golden Cove), vpshufb can often only execute on Port 5. This creates a "Port 5 Pressure" bottleneck where the CPU has plenty of compute power (ALUs) but can't rearrange data fast enough. 
Your Bit-Sliced approach shifts the workload:

    Mask Loading (kmov): Runs on Port 0/1/5 (Distributed).
    Expansion (vpmovm2b): Runs on Port 5 (but is cheaper than shuffle).
    Merge (vmov): Runs on Memory Ports or ALU ports depending on immediate encoding.

The Assembly Implementation
Here is the optimized AVX-512 sequence for your proposal. It reduces the expansion cost to effectively 2 instructions per 64 weights.
nasm

; Inputs:
; k1 = Negative Mask (Bits set where W = -1)
; k2 = Positive Mask (Bits set where W = +1)
; zmm_one = Register pre-filled with 1s (or use immediate embedding)

; 1. Expand Negatives (and clear Zeros)
; Generates 0xFF (-1) for set bits, 0x00 (0) for clear bits.
vpmovm2b zmm_weights, k1 

; 2. Merge Positives
; Overwrites with 1s ONLY where k2 is set. Preserves the rest.
vmovdqu8 zmm_weights {k2}, zmm_one 

; 3. Dot Product (Accumulate)
vpdpbusd zmm_accum, zmm_weights, zmm_activations

Use code with caution.
The Trade-Off: Storage Format
The only reason this isn't the default in generic libraries is File Format Compatibility.

    Standard (GGUF/GGML): Stores weights as Packed Integers (e.g., 2-bit chunks: 01, 11, 00 adjacent in memory).
    Your Approach: Requires Interleaved Bit-Planes. You must store 64 bits of "Mask A" followed by 64 bits of "Mask B".
    Consequence: You cannot simply mmap a standard GGUF file. You would need a conversion step to "transpose" the weights into this planar format. However, for a dedicated high-performance runtime, this conversion is a one-time cost that yields significant throughput gains. 

[msftbitnet.md](https://github.com/user-attachments/files/25830786/msftbitnet.md)


## Comments (2)

### Comment by @jaynu2 at 2026-03-09T03:23:46Z

I actually was trying to sugges to use the 8 bit integer broadcast instructions,  but gemini had its own idea. I have no idea if one way or the other is preferable, but I suppose this might be more commonly available.


VPBROADCASTB
	
8-bit Integer (byte)
	
vpbroadcastb zmm1 {k1}, al
How the Mask Works
The masking mechanism in AVX-512 (EVEX encoding) controls the destination update logic.

    Merge Masking ({k1}): Elements selected by the mask (bit = 1) are overwritten with the broadcast value. Elements not selected (bit = 0) preserve their previous value.
    Zero Masking ({k1}{z}): Elements selected by the mask are overwritten. Elements not selected are zeroed out

So, I ran this by gemini again, and this instruction appears acceptable also.

[msftbitnet2.md](https://github.com/user-attachments/files/25831257/msftbitnet2.md)


### Comment by @jaynu2 at 2026-03-12T16:38:24Z

Additional gemini ai comment on a possible RISC-V implementation of the suggestion, using an equivalent to the broadcast implementation in x86:

In RISC-V Vector (RVV 1.0), there is no direct "Move Mask to Byte" instruction because Masks are already Vector Registers (typically v0).
However, you are right that the "Broadcast" logic is the correct path. The specific instruction you are looking for is vmerge.vim (Vector Merge Immediate). It is the workhorse for conditional selection and serves as the direct equivalent to your AVX-512 "Zero/Merge" logic.
The RISC-V Equivalent: vmerge
The vmerge instruction is functionally a "Select" operation. It takes two sources and selects between them based on the mask bit.

    Instruction: vmerge.vim vd, vs2, imm, v0
    Logic:
        If Mask (v0) bit is 0: Copy value from vs2 (False source).
        If Mask (v0) bit is 1: Write imm (True source).

This allows you to implement your 2-Step Planar Expansion perfectly, and it is arguably more readable than the AVX-512 version.
The RISC-V Assembly Kernel (RVV 1.0)
Here is how your "Bit-Sliced" weight expansion translates to RISC-V. We assume SEW=8 (8-bit elements).
assembly

; Setup (Run once)
; Clear a register to serve as the "Zero Source"
vmv.v.i v10, 0          ; v10 = [0, 0, 0, ...]

; --- The Loop ---

; 1. Expand Negatives (Load Mask A -> v0)
vlm.v v0, (a1)          ; Load 64 packed bits (Mask Neg) into v0

; Logic: Select '-1' if mask is 1. Select '0' (from v10) if mask is 0.
; This creates the {-1, 0} base layer.
vmerge.vim v12, v10, -1, v0 

; 2. Inject Positives (Load Mask B -> v0)
vlm.v v0, (a2)          ; Load 64 packed bits (Mask Pos) into v0

; Logic: Select '1' if mask is 1. Select 'previous value' (from v12) if mask is 0.
; This merges +1s into the {-1, 0} result.
vmerge.vim v12, v12, 1, v0 

; Result: v12 is now expanded to {-1, 0, 1}
; Ready for Dot Product (e.g., vmaqa.vv for accumulation)

Use code with caution.
Comparison: AVX-512 vs. RISC-V
Feature
	
AVX-512 Implementation
	
RISC-V Implementation
Negative Step	vpmovm2b (Expand Mask to Byte)	vmerge.vim (Select -1 or 0)
Positive Step	vmovdqu8 {k} (Masked Move)	vmerge.vim (Select 1 or Prev)
Zeroing	Implicit in vpmovm2b or {z} flag	Explicit via vs2 operand (v10=0)
Mechanism	Write Masking: "Don't touch the 0s"	Selection: "Pick between New and Old"
Why this fits your Proposal
Your original insight—using Bit-Sliced (Planar) Weights—is actually more portable to RISC-V than the standard "Packed" format.

    Packed (2-bit): RISC-V has no dedicated vpshufb equivalent that works exactly like Intel's shuffle for small sub-byte elements. Unpacking 2-bit integers in RISC-V requires a slow sequence of shifts and vand.
    Planar (Your Idea): The code above is extremely fast on RISC-V hardware because vmerge is a simple ALU operation.

