# PR #486: fix: move sys.exit(1) inside except block in run_command()

- state: closed
- author: @Jah-yee
- created_at: 2026-03-18T06:57:10Z
- updated_at: 2026-07-01T12:50:10Z
- closed_at: 2026-07-01T12:50:10Z
- url: https://github.com/microsoft/BitNet/pull/486

---

The sys.exit(1) was incorrectly placed outside the except block in setup_env.py, causing it to run unconditionally after the try/except completes, regardless of whether the command succeeded or failed.

**Before:**
```python
except subprocess.CalledProcessError as e:
    logging.error(f"Error occurred while running command: {e}")
sys.exit(1)  # Runs always!
```

**After:**
```python
except subprocess.CalledProcessError as e:
    logging.error(f"Error occurred while running command: {e}")
    sys.exit(1)  # Runs only on error
```

Fixes: #447
